# Contributing

Contributions are dedicated to the public domain under the [Unlicense](LICENSE).
Sign commits with `git commit -s`.

```bash
swift test
scripts/build.sh
```

Keep `SionCore` free of AppKit and Core Graphics. Views call editor commands;
documents call archive services. Format changes require a schema version, spec
update, migration, and shared fixture.
